---
/* Just a heading with accent slashes. Content is passed via slot (already translated). */
---

<h2 class="font-headline-lg text-[22px] md:text-[28px] leading-[1.2] font-bold uppercase mb-lg">
  <span class="text-secondary" aria-hidden="true">//</span> <slot />
</h2>
